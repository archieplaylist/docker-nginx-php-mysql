services:
    proxy:
        image: nginx:alpine
        container_name: nginx-proxy
        volumes:
            - "./etc/nginx/proxy.conf:/etc/nginx/conf.d/default.conf:z"
            - "./etc/ssl:/etc/ssl:z"
        ports:
            - "80:80"
            - "443:443"
        restart: always
        depends_on:
            - web
        networks:
            - websvr
    web:
        image: nginx:alpine
        container_name: websvr
        volumes:
            - "./etc/nginx/default.conf:/etc/nginx/conf.d/default.conf:z"
            - "./etc/ssl:/etc/ssl:z"
            - "./web:/var/www/html:z"
            - "./etc/nginx/default.template.conf:/etc/nginx/conf.d/default.template:z"
        expose:
            - "80"
        environment:
            - NGINX_HOST=${NGINX_HOST}
        command: /bin/sh -c "envsubst '$$NGINX_HOST' < /etc/nginx/conf.d/default.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
        restart: always
        depends_on:
            - php
            - mariadb
        networks:
            - websvr
    php:
        image: archieplaylist/php-fpm:${PHP_VERSION}
        container_name: php
        restart: always
        volumes:
            - "./etc/php/php.ini:/usr/local/etc/php/conf.d/php.ini:z"
            - "./web:/var/www/html:z"
        networks:
            - websvr
    composer:
        image: "composer"
        volumes:
            - "./web/app:/app:z"
        command: install
        networks:
            - websvr
    mariadb:
        image: mariadb:${MARIADB_VERSION}
        container_name: ${MARIADB_HOST}
        restart: always
        env_file:
            - ".env"
        environment:
            - MARIADB_DATABASE=${MARIADB_DATABASE}
            - MARIADB_ROOT_PASSWORD=${MARIADB_ROOT_PASSWORD}
            - MARIADB_USER=${MARIADB_USER}
            - MARIADB_PASSWORD=${MARIADB_PASSWORD}
        ports:
            - "3306:3306"
        volumes:
            - "./data/db/mariadb:/var/lib/mysql:z"
        networks:
            - websvr
    postgres:
        image: postgres:${POSTGRES_VERSION}
        container_name: ${POSTGRES_HOST}
        restart: always
        env_file:
            - ".env"
        environment:
            - POSTGRES_DB=${POSTGRES_DB}
            - POSTGRES_USER=${POSTGRES_USER}
            - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
        ports:
            - "5432:5432"
        volumes:
            - "./data/db/postgres:/var/lib/postgresql/data:z"
        profiles:
            - pg
        networks:
            - websvr

networks:
    websvr:
        driver: bridge
        ipam:
            config:
                - subnet: 172.28.0.0/16